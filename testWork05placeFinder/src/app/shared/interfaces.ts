export interface DataLocation {
  query: string;
  radius: number;
  lat: number;
  lng: number;
}

export interface DataPlace {
  address: string;
  city: string;
  lat: number;
  lng: number;
  name: string;
}
